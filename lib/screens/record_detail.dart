import 'package:flutter/material.dart';

class RecordDetail extends StatelessWidget {
  const RecordDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("기록 상세")),
      body: Column(),
    );
  }
}
